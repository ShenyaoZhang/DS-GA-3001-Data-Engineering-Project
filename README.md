# DS-GA-3001 Data Engineering Project

- **`emotions_rec/`** — **binary emotion (default: joy vs rest)** on `dair-ai/emotion`, branch **`emotion-rec-joy`**. See `emotions_rec/README.md` and `emotions_rec/notebooks/emotion_rec_joy_repro.ipynb`.
- **`LTS/`** — Reuters / lean-to-sample style pipeline; kept aligned with **`main`** (vanilla). Not used by the joy emotion workflow.

Root `requirements.txt` applies to the Python stacks above where noted in subfolder READMEs.
