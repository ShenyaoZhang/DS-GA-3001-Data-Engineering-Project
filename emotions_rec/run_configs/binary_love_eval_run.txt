# Official test-set eval. Pick the MODEL_DIR from the trained run (iteration + bandit with best validation f1_pos).
#
# Threshold tuning reduces majority-class dominance in reported F1(pos): optimizer runs on VALIDATION CSV.
python -u src/eval_emotion_binary.py \
  -test_path "data/processed/emotions_love_test.csv" \
  -tune_threshold_val_path "data/processed/emotions_love_validation.csv" \
  -model_path "MODEL_DIR" \
  -target_emotion "love" \
  -base_model "bert-base-uncased" \
  -max_length 128
